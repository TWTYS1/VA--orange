# PRD-职场嘴替_v2

> 面向第一阶段 PR 的产品需求文档。本文以当前 VoiceFlow 实现方案为主，吸收竞品分析与原 PRD 中有价值的场景、验证指标和风险判断，同时压缩掉真实 ASR、真实 LLM、移动端输入法扩展、分享面板、埋点等会让第一阶段冗余的内容。

---

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档版本 | V2.0 |
| 更新日期 | 2026-05-25 |
| 产品代号 | 职场嘴替 / VoiceFlow |
| 当前阶段 | 第一阶段 MVP |
| 技术形态 | Vite React 单页应用 |
| 当前策略 | Mock 优先，先验证产品闭环 |
| 配套文档 | `voice-input-pm-analysis.html`、`竞品分析报告-语音输入助手MVP.md`、`ARCHITECTURE.md` |

---

## 2. 一句话需求

用户输入或模拟口述一段职场意图，系统根据沟通对象和回复风格，生成结构化理解、多版本可发送回复和沟通风险提示，帮助用户降低从“想表达”到“得体回复”的思考和修改成本。

---

## 3. 核心洞察

职场语音输入的效率瓶颈，不只是语音识别速度，也不是普通 AI 润色能力，而是：

- 用户知道大概要说什么，但不知道怎么说得体。
- 口述内容通常是散的，不能直接复制发送。
- 给领导、同事、客户、下属说同一件事，语气和结构不同。
- 职场回复慢，慢在措辞、补结构、控制风险，而不只是打字慢。

因此第一阶段不拼“大厂已经解决”的能力：

| 不作为核心差异化 | 原因 |
|---|---|
| 更准 ASR | 主流输入法和 ASR 服务已经成熟，72 小时内不应硬拼识别率。 |
| 更多方言 | 数据和模型壁垒高，且职场 MVP 普通话输入即可验证主要假设。 |
| 通用 AI 润色 | 大模型基础能力，不能单独构成产品创新。 |
| 系统级输入法 | 工程成本高，72 小时内风险大，不利于先验证产品价值。 |

第一阶段真正验证的是：

> 用户是否接受“说意图，得回复”的工作流，而不是“我说什么，你逐字记录”的传统转录工作流。

---

## 4. 目标用户与场景

### 4.1 目标用户

| 维度 | 说明 |
|---|---|
| 核心用户 | 高频使用微信、飞书、钉钉、邮件进行工作沟通的职场人 |
| 典型角色 | 产品、运营、销售、客服、项目管理、团队负责人 |
| 使用动机 | 快速生成得体回复，减少反复措辞和修改 |
| 当前行为 | 重要消息先打草稿，改几遍后再复制发送 |

### 4.2 第一阶段主场景

第一阶段只做“职场回复生成”，不做会议纪要、学习笔记、长辈聊天等扩展场景。

| 场景 | 用户口述例子 | 期望输出 |
|---|---|---|
| 客户报价延期 | 供应商还没确认，今天报价给不了，最快明天下午 | 专业礼貌说明原因、时间和后续跟进 |
| 向领导同步延期 | 数据没跑完，方案要延期两天 | 正式汇报当前阻塞、延期原因和下一步 |
| 同事排期沟通 | 这个需求这周做不完，下周再排 | 简洁自然说明排期边界 |
| 推动下属执行 | 这个材料今天先整理第一版，明天上午我看完以后再补细节 | 明确任务、时间点和后续协作方式 |

---

## 5. 第一阶段范围

### 5.1 必须做

| 编号 | 功能 | 说明 | 当前实现状态 |
|---|---|---|---|
| F1 | 文本输入 / 模拟语音输入 | 用文本框和样例模拟口述输入，不声称真实 ASR 已完成。 | 已实现 |
| F2 | 沟通对象选择 | 支持领导、同事、客户、下属。 | 已实现 |
| F3 | 回复风格选择 | 支持简洁、礼貌、推进、正式。 | 已实现 |
| F4 | 结构化理解 | 输出结论、原因、时间、下一步。 | 已实现 |
| F5 | 多版本回复 | 输出推荐版、简短版、增强推进版。 | 已实现 |
| F6 | 风险提示 | 提示时间、下一步、承诺、正式对象表达等风险。 | 已实现 |
| F7 | 编辑与复制 | 推荐版可编辑，生成结果可复制。 | 已实现 |
| F8 | 文档交付 | README、架构说明、产品规划、竞品报告。 | 已实现 |

### 5.2 第一阶段明确不做

| 不做项 | 原因 | 后续归属 |
|---|---|---|
| 真实 ASR | 浏览器权限、兼容性、识别稳定性会增加第一阶段风险。 | V2 |
| 真实 LLM | 需要 API Key、费用、网络、隐私说明，容易干扰基础 Demo。 | V3 或 V2 后半 |
| 微信/钉钉直接发送 | Web MVP 难稳定调起目标 App，复制已能验证核心价值。 | V3 |
| iOS/Android 输入法扩展 | 工程量大，不适合第一阶段。 | V4 |
| 用户登录、历史记录、个人风格学习 | 与第一阶段核心假设无关。 | V3/V4 |
| 埋点系统 | 当前评审更看重可运行 Demo、README、PR 记录。 | V2 |

---

## 6. 产品流程

```text
用户输入 / 模拟语音
  -> 选择沟通对象
  -> 选择回复风格
  -> 点击生成职场回复
  -> 查看结构化理解
  -> 编辑推荐回复或复制其他版本
  -> 根据风险提示确认是否需要补充信息
```

### 6.1 页面结构

| 区域 | 作用 |
|---|---|
| 顶部标题区 | 说明产品定位：职场语音意图输入助手。 |
| 输入区 | 输入口述内容、模拟语音输入、清空、生成。 |
| 语境选择区 | 选择沟通对象和回复风格。 |
| 结构化理解区 | 展示系统对意图的拆解。 |
| 回复结果区 | 展示推荐版、简短版、增强推进版。 |
| 风险提示区 | 提醒缺时间、下一步不清、承诺偏强、正式对象表达等问题。 |

---

## 7. 数据模型与工程接口

第一阶段核心接口保持稳定，方便后续从 Mock 切换到真实 LLM。

```js
generateReply({
  rawText: string,
  audience: 'leader' | 'peer' | 'client' | 'subordinate',
  tone: 'concise' | 'polite' | 'driving' | 'formal'
})
```

返回：

```js
{
  summary: {
    conclusion: string,
    reason: string,
    time: string,
    nextStep: string
  },
  replies: {
    recommended: string,
    short: string,
    assertive: string
  },
  risks: string[]
}
```

当前阶段该接口由 `src/lib/generateReply.js` 中的 Mock 规则引擎实现。后续接入真实 LLM 时，只需要把该接口改成异步 adapter，UI 不需要整体重写。

---

## 8. 验收标准

### 8.1 第一阶段 PR 验收

| 编号 | 验收标准 | 验证方式 |
|---|---|---|
| AC1 | 首页直接进入可用产品界面，不是营销页。 | 打开本地页面检查。 |
| AC2 | 输入客户报价延期场景，选择客户 + 礼貌，能生成专业礼貌回复。 | 手工测试。 |
| AC3 | 输入方案延期场景，选择领导 + 正式，能生成汇报型回复。 | 单元测试 + 手工测试。 |
| AC4 | 输入排期沟通场景，选择同事 + 简洁，能生成自然协作回复。 | 手工测试。 |
| AC5 | 空输入点击生成，出现明确提示，不生成空结果。 | 单元测试 + 手工测试。 |
| AC6 | 推荐版可编辑，复制按钮可用并显示复制状态。 | 手工测试。 |
| AC7 | README 写清产品定位、运行方式、Mock 说明、隐私说明、后续迭代。 | 文档检查。 |
| AC8 | 架构文档说明模块边界、数据流和替换点。 | 文档检查。 |
| AC9 | `npm test` 通过。 | 自动测试。 |
| AC10 | `npm run build` 通过。 | 构建验证。 |

### 8.2 产品验证指标

这些指标不是第一阶段 PR 的硬性阻塞，但用于后续用户测试：

| 假设 | 指标 | 建议阈值 |
|---|---|---|
| 用户愿意说意图而不是逐字打字 | “比手打省心/更快”主观评分 | 5 人中至少 4 人评分 4/5 以上 |
| 生成结果足够可用 | “可直接发送 + 微调可用”比例 | 70% 以上 |
| 职场回复是高频场景 | 用户自发选择的测试任务 | 80% 来自工作消息回复 |
| 风险提示有价值 | 用户认为风险提示“有帮助” | 5 人中至少 3 人认可 |

---

## 9. 第一阶段 PR 规范

第一阶段只提交一个清晰 PR，目标是让评审可以运行、阅读、验证。

### 9.1 PR 标题

```text
feat: add VoiceFlow workplace reply MVP
```

### 9.2 PR 描述模板

```markdown
## 功能描述

新增 VoiceFlow 职场语音意图输入助手 MVP。用户可以输入或模拟口述一段职场意图，选择沟通对象和回复风格，生成结构化理解、多版本可发送回复和沟通风险提示。

## 实现思路

- 使用 Vite + React 实现单页应用。
- 使用 Mock 规则引擎 `generateReply(input)` 验证“口述意图 -> 职场回复”的核心链路。
- 将 UI 组件、样例数据、生成逻辑和测试拆分，便于后续替换真实 ASR / LLM。
- 当前版本不接真实语音识别和真实大模型，避免第一阶段被 API Key、网络、费用和隐私问题阻塞。

## 测试方式

- `npm test`
- `npm run build`
- 手工验证：
  - 客户报价延期：客户 + 礼貌
  - 方案延期：领导 + 正式
  - 排期沟通：同事 + 简洁
  - 空输入提示
  - 编辑与复制

## 文档

- `README.md`
- `ARCHITECTURE.md`
- `voice-input-pm-analysis.html`
- `竞品分析报告-语音输入助手MVP.md`
- `PRD-职场嘴替_v2.md`
```

---

## 10. 后续路线

| 阶段 | 目标 | 关键动作 |
|---|---|---|
| 第一阶段 | 可运行 MVP + 清晰文档 + 第一版 PR | 保持 Mock 优先，证明产品闭环。 |
| 第二阶段 | 增强产品可信度 | 增加更多职场场景、风险规则、Demo 脚本和录屏材料。 |
| 第三阶段 | 真实能力接入 | 封装真实 LLM adapter；可选接 Web Speech API。 |
| 第四阶段 | 输入法形态探索 | 浏览器插件、桌面悬浮窗、移动端输入法扩展。 |

---

## 11. 本版相对原 PRD 的取舍

### 保留

- “职场嘴替”定位。
- “说意图，得回复”的核心范式。
- 上级、平级、客户等关系语气差异。
- 竞品分析中的真假创新判断。
- 用户验证指标和 Go/No-Go 思路。

### 调整

- 将真实 ASR、真实 LLM 从第一阶段 P0 下沉为后续阶段。
- 将 5 种场景模板收敛为当前已实现的 4 个演示场景，并保留扩展空间。
- 将“发送到微信/钉钉”收敛为“一键复制”，避免 Web MVP 交付不稳定。
- 将复杂性能指标、埋点和移动端录音异常处理从第一阶段移除。

### 删除或延后

- API 成本预算。
- 录音波形、ASR 置信度、Share Sheet、埋点系统。
- iOS/Android 输入法扩展。
- 多轮上下文记忆、个人风格学习、离线模式。

---

## 12. 结论

第一阶段的目标不是证明我们能做出完整输入法，而是证明“职场回复场景里，用户需要的不是单纯转录，而是把口述意图整理成可发送文本”的产品判断成立。

因此本 PRD 将第一阶段范围固定为：

```text
Mock 输入 + 关系/语气选择 + 结构化理解 + 多版本回复 + 风险提示 + README/架构/测试
```

这个范围足够小，可以满足活动 PR 规范；同时边界清晰，可以自然演进到真实 ASR、真实 LLM 和输入法形态。
