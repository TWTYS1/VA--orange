# "职场嘴替" 72小时MVP — 精简PRD

> **配套文档**：`竞品分析报告-语音输入助手MVP.md`（竞品洞察、产品空位、差异化论证详见该文档）
>
> **PRD类型**：小功能PRD（<5人天）—— 保留需求概述、功能需求、验收标准

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-05-25 |
| 文档状态 | 草稿 |
| 项目代号 | "职场嘴替" — Workplace Voice Intent Assistant |
| 开发周期 | 72小时（3天） |

---

## 1. 需求背景

### 1.1 一句话需求

> 用户对着手机说出想表达的意思（而非逐字照念），系统自动输出一段得体、结构化、可直接发送的微信/钉钉职场回复。

### 1.2 为什么要做

**数据支撑**：
- 83%的管理者通过群聊回复方式预判下属潜力；96%的职场人因不当回复影响晋升（《2025中国职场沟通效率报告》）
- 76.8%的管理者对"好的""收到"类简单回复不满（同上）
- 使用结构化回复方式的员工晋升概率高42%（猎头公司调研）
- 84.7%职场人下班后仍需回复工作消息，"措辞焦虑"是高频隐性痛点（2024《工人日报》）
- 讯飞输入法月活1.39亿，搜狗语音日请求近20亿次——语音输入习惯已大规模养成

**竞品空白**（详见竞品报告 §4）：
- 现有所有语音产品都是"转录驱动"（我说→你记→我改），无人做"意图驱动"（我说意图→你组织语言→我确认发送）
- 大厂组织边界导致这个交叉地带无人覆盖：输入法团队管输入效率、AI团队管模型能力、企微/钉钉团队管协同——没人做"职场沟通意图理解"

### 1.3 目标用户

| 维度 | 核心用户 |
|------|---------|
| 身份 | 日常使用微信/钉钉进行大量工作沟通的知识工作者 |
| 典型画像 | 互联网PM/运营/销售/管理者，工作3年+ |
| 日均工作消息 | ≥30条（[预估]） |
| 核心痛点 | "不是不知道说什么，而是不知道怎么说得体"——措辞时间>表达时间 |
| 现状行为 | 重要消息先在备忘录打草稿改2-3遍，或干脆发语音/打电话降低文字压力 |

---

## 2. 产品目标与范围

### 2.1 72小时MVP目标

**验证4个核心假设**（全部通过=进入V1）：

| # | 假设 | 验证指标 | 成功阈值 |
|---|------|---------|---------|
| H1 | 用户愿意用"说意图"替代"打字措辞" | 事后问卷"比手打快"评分（1-5） | ≥4/5用户评4分以上 |
| H2 | 意图→回复准确率>70%即可让用户觉得可用 | 50条测试输出人工评分（可用/微调/重写） | ≥70%属于可用+微调 |
| H3 | 微信/钉钉回复是最高频场景 | 用户自发选择的任务类型 | ≥80%为工作消息回复 |
| H4 | 用户认可"省措辞时间"的价值 | "愿意推荐给同事"的比例 | ≥4/5用户表示愿意 |

### 2.2 MVP范围

**必须做（P0）**：

| 功能 | 一句话描述 | 预估工时 |
|------|-----------|---------|
| F1 语音输入 | 按住录音→ASR转文字→展示识别结果 | 4h |
| F2 意图理解+结构化输出 | 将口语意图文本+场景参数送入LLM→输出结构化回复卡片 | 8h（核心Prompt调优） |
| F3 场景选择 | 5种回复结构模板：汇报/请假/拒绝/催促/感谢 | 3h |
| F4 语气切换 | 3种语气梯度：对上级（正式得体）/对平级（友好自然）/对客户（专业礼貌） | 2h |
| F5 结果操作 | 一键复制、发送（调起微信/钉钉分享）、"换一种说法"重新生成 | 3h |
| F6 基础UI | 录音界面+结果展示卡片+场景/语气选择器 | 6h |

**明确不做**：
- 不自研ASR（接入讯飞API或OpenAI Whisper API）
- 不自研LLM（接入DeepSeek-V3或Claude API）
- 不做方言、不做离线、不做历史记录、不做个人风格学习
- 不做多轮对话、不做上下文记忆
- 不做用户注册/登录（MVP期间）
- 不做iOS键盘扩展（V1再做，MVP接受"切App"摩擦）

---

## 3. 用户场景

### 场景1：向上级汇报延期（正常流程）

> **人物**：小李，28岁，互联网产品经理
> **时间**：周三下午3点，刚发现数据口径有问题，报表要延期一天
> **场景**：老板微信问"那个报表今天能出吗？"

**操作流程**：

1. 小李打开"职场嘴替"App，选择场景「汇报/同步」，选择语气「对上级」
2. 按住录音按钮，口述："告诉老板数据口径有点问题报表要晚一天明天下午三点前给过去"
3. 松手，系统ASR转文字，展示识别结果："告诉老板数据口径有点问题报表要晚一天明天下午三点前给过去"
4. 1-3秒后（LLM处理），展示结构化回复卡片：

> **输出（可直接发送）**：
> 老板，报表这边遇到一个数据口径问题，需要重新核对一下。预计延迟一天，明天下午3点前发您终版。抱歉给您添麻烦了，我尽快处理。[🙏]

5. 小李点击「复制」，切回微信，粘贴，发送。
6. （可选）如果觉得语气偏软，点击「换一种说法」，输出更简洁版本。

### 场景2：拒绝跨部门不合理需求（边界场景）

> **人物**：小王，30岁，后端开发组长
> **场景**：运营同事在群里@他，要求在周五前上线一个没有排期的功能

**操作流程**：

1. 小王打开App，选择场景「拒绝/推辞」，选择语气「对平级」
2. 口述："告诉运营这个需求没排期这周肯定上不了可以先评审排到下个迭代"
3. 系统输出：

> **输出**：
> 这个需求目前还没有排进这期迭代。建议先走评审流程，如果优先级没问题，可以安排到下个迭代的第一个位置。这周的资源确实已经排满了，硬插的话会影响其他需求的交付质量。理解你的着急，我们尽快推进评审。[🤝]

4. 小王点击「发送」，通过系统分享直接发到对应微信群。

---

## 4. 功能需求（开发规格）

### 4.1 F1：语音输入

**Input**：
- 触发：点击/按住录音按钮（移动端）
- 音频参数：16kHz采样率，单声道，格式由ASR API决定

**Process**：
1. 客户端采集麦克风音频（录音中展示波形动画）
2. 录制停止后，将音频数据通过HTTPS发送至ASR API
3. ASR返回识别文本
4. 展示文本在输入预览区（可手动编辑修正）

**Output**：
- 识别文本字符串（展示在预览区）
- 若用户手动编辑，以编辑后文本为准

**Exception**：
| 异常 | 处理 |
|------|------|
| 录音时长<1秒 | 不调用API，toast提示"请至少说1秒" |
| 录音时长>120秒 | 自动截断，提示"已截取前2分钟" |
| ASR API超时（>10秒） | 提示"识别超时，请重试或手动输入"，提供重试按钮 |
| ASR API返回空结果 | 提示"未识别到语音内容，请重试" |
| 麦克风权限未授予 | 引导用户前往系统设置开启权限 |
| 环境噪音过大（API返回低置信度） | 展示识别结果但标记低置信区间（灰色文字），提示"识别置信度较低，建议编辑确认" |

---

### 4.2 F2：意图理解+结构化输出（核心功能）

**Input**：
- 口语文本（来自F1或手动输入）
- 场景参数：场景类型（枚举值）+ 语气梯度（枚举值）+ 可选上下文（聊天记录粘贴）
- 可选：对话对象角色（上级/平级/下属/客户）

**Process**（Prompt驱动，无需自研模型）：

```
[System Prompt 核心结构]

你是一个职场沟通助手。你的任务是将用户的口语化意图转换成得体、结构清晰、可直接发送的职场回复。

规则：
1. 删除口语赘余（"嗯""那个""就是"），保留核心意图
2. 补充必要的礼貌用语和过渡词
3. 根据指定场景类型，套用合适的结构
4. 根据指定语气梯度，调整用词和称呼
5. 如有歧义，选择最得体的理解，不要询问
6. 输出长度控制在适合微信/钉钉单条消息的范围（≤200字）

场景类型：{scene_type}
语气梯度：{tone_level}
对话对象：{target_role}

用户口述：{user_input}

请输出可直接发送的回复文本。
```

**Output**：
- 结构化回复文本（≤200字）
- 如适用，附emoji建议（可选）

**Exception**：
| 异常 | 处理 |
|------|------|
| LLM API超时（>8秒） | 展示loading动画，超时后提示"生成超时，请重试" |
| LLM返回空/异常 | 展示"生成失败，请重试"，记录错误日志 |
| 输入文本<2字 | 不调用LLM，提示"请至少输入想要表达的内容" |
| 输出含敏感词 | 过滤后重新生成，标记"内容已自动调整" |

**场景类型枚举**：

| 场景ID | 场景名 | 输出结构模板 | 示例输出首句 |
|--------|--------|------------|------------|
| report | 汇报/同步 | 现状→原因→计划→承诺时间 | "XX这边有个情况同步一下" |
| leave | 请假/申请 | 事由→时间→交接安排→联系方式 | "需要请个假，X号到X号" |
| decline | 拒绝/推辞 | 理解→难处→替代方案→善意结尾 | "理解这个需求很急" |
| remind | 催促/跟进 | 背景→现状→期望行动→时间点 | "想同步一下XX的进度" |
| thanks | 感谢/回应 | 感谢→具体受益点→后续行动/善意 | "收到，非常感谢！" |

**语气梯度枚举**：

| 语气ID | 语气 | 适用对象 | 特征词示例 |
|--------|------|---------|-----------|
| formal | 正式得体 | 上级/客户 | "请知悉""辛苦""烦请""抱歉给您添麻烦了" |
| friendly | 友好自然 | 平级同事 | "哈""嗯嗯""好的呀""谢谢谢谢" |
| professional | 专业礼貌 | 客户/跨部门 | "您好""请""感谢配合""祝工作顺利" |

---

### 4.3 F3/F4：场景选择与语气切换

**交互设计**：
- 录音按钮上方展示两个快速选择器（横向滚动chip）
- 场景选择器：5个chip（汇报/请假/拒绝/催促/感谢），默认"汇报"
- 语气选择器：3个chip（对上级/对平级/对客户），默认"对平级"
- 选择后chip高亮，影响LLM Prompt参数
- 支持在录音前切换，也支持在结果展示后切换（重新生成）

---

### 4.4 F5：结果操作

**Output展示**：
- 白色卡片式展示，标题"💬 回复建议"，正文为结构化回复
- 卡片下方展示语气/场景标签（如"📋 汇报 · 👔 对上级"）
- 底部操作栏三个按钮：

| 按钮 | 行为 | 图标 |
|------|------|------|
| 📋 复制 | 一键复制回复文本到剪贴板，展示"已复制"toast | Copy |
| 📤 发送 | 调起系统分享面板（Share Sheet），可选微信/钉钉 | Share |
| 🔄 换一种 | 重新调用LLM（保持原输入+原参数），生成新回复 | Refresh |

**"换一种"逻辑**：调用LLM时增加参数 `temperature +0.1` 或添加 `"请换一种表达方式"` 到Prompt，确保输出不同。

---

### 4.5 F6：基础UI

**页面结构**（单页应用）：

```
┌─────────────────────────────┐
│     职场嘴替（标题栏）        │
├─────────────────────────────┤
│                             │
│  场景：[汇报▾] [请假] [拒绝]  │  ← 横向滚动chip
│  语气：[对上级▾] [对平级]     │  ← 横向滚动chip
│                             │
├─────────────────────────────┤
│                             │
│    ┌─────────────────┐     │
│    │                 │     │
│    │   按住录音按钮   │     │  ← 录音区域（未录音状态）
│    │   🎤            │     │
│    │                 │     │
│    └─────────────────┘     │
│                             │
│  "或直接输入文字意图..."     │  ← 文字输入兜底
│                             │
├─────────────────────────────┤
│  ASR识别结果预览区           │
│  ┌─────────────────────────┐│
│  │ 告诉老板数据延期...      ││  ← 可编辑
│  └─────────────────────────┘│
├─────────────────────────────┤
│  回复卡片                   │
│  ┌─────────────────────────┐│
│  │ 老板，报表这边遇到了...  ││
│  │                         ││
│  │ [📋复制] [📤发送] [🔄换]││
│  └─────────────────────────┘│
└─────────────────────────────┘
```

---

## 5. 非功能需求

### 5.1 性能（AI产品特化）

| 指标 | 目标值 | 测量方法 |
|------|--------|---------|
| 端到端延迟（P95） | ASR 1.5s + LLM 3.5s ≤ 5秒总延迟 | 客户端计时器 |
| ASR延迟 | ≤1.5秒（短句<10字） | API响应时间 |
| LLM推理延迟 | ≤4秒（含网络RTT） | API first-token时间 |
| 录音→结果展示总时间 | P50 ≤3秒，P95 ≤5秒 | 全链路计时 |

### 5.2 成本预算（72小时MVP）

| 项目 | 预估用量 | 单价 | 预估成本 |
|------|---------|------|---------|
| ASR API（讯飞） | 500次调用（开发+测试+演示）| 免费额度内 | ¥0 |
| LLM API（DeepSeek-V3）| 1000次调用 | ~¥0.001/1K tokens | ¥10-20 |
| LLM API（Claude Haiku 备选）| 1000次调用 | ~$0.25/1M input tokens | ~$1-2 |
| **总预算** | | | **¥50以内** |

### 5.3 技术选型

| 层 | 方案 | 理由 |
|----|------|------|
| **前端** | React + Vite（PWA）/ Flutter（跨平台）| PWA最快出Demo；Flutter可复用iOS+Android |
| **ASR** | 讯飞语音听写API | 中文准确率98%+，有免费额度，支持中英混输 |
| **LLM** | DeepSeek-V3 API | 中文职场表达好，成本极低，国内访问稳定 |
| **LLM备选** | Claude Haiku 4.5 | 如需更强语义理解，成本略高但质量更稳定 |
| **后端** | 无（纯客户端调用API）| 72小时内不建后端，Prompt内嵌客户端代码或通过Edge Functions中转 |
| **部署** | Vercel / Cloudflare Pages | 免费、快速部署、HTTPS自带 |

> **注意**：如果API Key不能直接暴露在前端，需要搭建一层简单的Edge Function做代理（+2h工作量）。

---

## 6. 验收标准

| 编号 | 验收标准 | 验证方式 |
|------|---------|---------|
| AC1 | 用户口述中文短句（10-30字），P95总延迟≤5秒完成"录音→展示回复卡片" | 手动计时，20次测试 |
| AC2 | ASR识别中文准确率≥95%（安静环境，标准普通话，50句测试集） | 手工对比原始文本与识别文本 |
| AC3 | 5种场景类型全部可用，每种至少2个测试用例通过 | 手工验证，10/10通过 |
| AC4 | 3种语气梯度输出在盲测中可被正确识别（≥80%准确率） | 10人盲测，每人判断3条回复的对应语气 |
| AC5 | 50条测试输出（10人×5场景），"可直接发送+微调可用"比例≥70% | 人工评分 |
| AC6 | "换一种说法"按钮触发后，输出与上一轮不同（相似度<80%） | 手工验证，连续3次不重复 |
| AC7 | 一键复制成功，剪贴板内容=展示内容 | 手工验证 |
| AC8 | 在iPhone 12/13（或同等Android设备）上UI正常渲染，无崩溃 | 实机测试 |
| AC9 | 手动输入文字作为语音输入替代路径可用 | 手工验证 |
| AC10 | 录音时展示音频波形动画（≥20fps） | 目视验证 |

---

## 7. 数据埋点（最小集）

| 事件名称 | 触发时机 | 字段 | 目的 |
|---------|---------|------|------|
| `record_start` | 用户按下录音按钮 | timestamp, scene_type, tone_level | 使用量 |
| `record_complete` | 录音结束 | audio_duration_sec, asr_latency_ms | 录音行为分析 |
| `result_display` | 回复卡片渲染完成 | total_latency_ms, llm_latency_ms, scene_type, tone_level, output_length | 核心性能监控 |
| `action_copy` | 用户点击复制 | output_id | 回复可用率（copy=认可） |
| `action_share` | 用户点击发送/分享 | output_id, share_target(wx/dingtalk/other) | 分发渠道分析 |
| `action_regenerate` | 用户点击"换一种" | output_id, regenerate_count | 首次输出不满意率 |
| `manual_edit` | 用户手动编辑了ASR结果 | edit_distance | ASR识别质量监控 |

---

## 8. 项目规划（72小时）

### 时间分配建议

| 时间段 | 任务 | 交付物 | 负责人建议 |
|--------|------|--------|-----------|
| **Day1 上午（4h）** | 技术环境搭建：PWA脚手架、API Key申请、ASR/LLM联调 | ASR→文本流程跑通 | 全栈开发 |
| **Day1 下午（4h）** | Prompt工程：5种场景×3种语气的Prompt模板+测试用例跑通 | 核心Prompt稳定、输出可用率>60% | 全栈（+PM协助Prompt调优） |
| **Day1 晚上（2h）** | UI骨架：录音按钮、场景/语气选择器、结果卡片基础样式 | 可点击的静态Demo | 前端 |
| **Day2 上午（4h）** | 核心流程串通：录音→ASR→LLM→卡片展示（端到端跑通） | 可用的端到端流程 | 全栈 |
| **Day2 下午（4h）** | UI完善+交互：卡片动画、复制/分享/换一种、错误状态、Loading动画 | 接近可演示的完整UI | 前端 |
| **Day2 晚上（2h）** | Prompt第二轮调优：基于端到端测试结果，将可用率提到>70% | 可用率达标 | PM+全栈 |
| **Day3 上午（4h）** | Bug修复+边缘处理：空状态、超时、权限拒绝、超长文本、特殊字符 | 边界情况覆盖 | 全栈 |
| **Day3 下午（4h）** | 部署+用户测试准备：部署到Vercel、准备5个测试任务卡、招募5-8名测试用户 | 在线可访问+测试方案 | 全栈+PM |
| **Day3 晚上（2h）** | 用户测试执行+数据整理+Go/No-Go判断 | 验证4个核心假设 | PM |

### 风险与应对

| 风险 | 应对 |
|------|------|
| ASR/LLM API申请审核超过1天 | Day1备用方案：用浏览器内置SpeechRecognition API（Chrome支持）+本地Mock数据先跑通UI |
| Prompt调优不达70%可用率 | 收缩MVP场景范围：Day2只做3种场景×2种语气（汇报+拒绝+感谢 × 上级+平级） |
| PWA在移动端录音兼容性问题 | 改用移动端浏览器测试，或临时切为纯手动输入+LLM的降级方案（语音先mock） |
| 招募不到5名测试用户 | 降低门槛：找同事/朋友3人即可，重点观察定性行为而非统计显著性 |

---

> **Go/No-Go判断**：Day3用户测试后，4项核心假设全部达标→进入V1（2-4周）；2-3项达标→调整方向后重新验证；≤1项达标→推倒假设重来。
>
> **配套文档**：详细竞品分析和差异化论证见 `竞品分析报告-语音输入助手MVP.md`
